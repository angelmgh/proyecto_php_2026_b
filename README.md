# Poyecto PHP

## Descripcion
este es un proyecto de una pagina donde se muestra hola mundo
junto con una imagen creada con IA

## P rerequisistos
1. debemos instalar apache2
2. sudo apt install apache2
3. debemos instalar php
4. sudo apt install php
5. demeos instalar git
6. sudo apt install git
   
